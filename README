Website is based off of MERN (Mongo, Express, React, Node) full-stack. 

(6/14/22)
(server/Users.js)
UserModel and UserSchema are created here. UserSchema includes (1)first_name, (2)last_name, (3)age, (4)email, (5)username, (6)password.

(server/index.js)
Functionality for login and creation has been added using Express POST method. Account Verification is done through UserModel.find(). Account info (login/creation) 
sends POST request to MongoDB.

(client/App.js)
Functionality for creating and logging in a user done through Axios POST. Account info goes to server/index.js and into MongoDB
